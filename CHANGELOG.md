# Changelog

## v0.5
- still in development...

## v0.4
- `search` command fixed
- added proper package versioning
- the shell prompt now stops covering the error message from the `update` command
- fixed the "Downloading..." section to have status message "[ OK ]"
- bug fixes (no more segfaults)
## v0.3.2
- a segfaulting `search` command added (searching by name)
- `version` command added
- `list` command now segfaults
- minor bug fixes (still segfaults)
- more status messages added: "[FAIL]" and "[INFO]"
## v0.2
- `remove` command added
## v0.1
- initial release
- `update` command added
- status message added: [ OK ]
- removed broken loading bars that appeared in the `install` command's stages
## v0.0
- first version
- `install` command added
- `list` command added
- `list_installed` command added
- added broken loading bars that appeared in the `install` command's stages
